server:
  port: ${TENANT_SERVICE_PORT:8083}

spring:
  application:
    name: service-tenant
  config:
    import: optional:nacos:${spring.application.name}.yml
  datasource:
    url: ${TENANT_DB_URL:jdbc:postgresql://10.193.102.2:5432/triobase}
    username: ${TENANT_DB_USERNAME:postgres}
    password: ${TENANT_DB_PASSWORD:pwd123}
    driver-class-name: org.postgresql.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: false
    table: flyway_schema_history_tenant
    baseline-on-migrate: true
    baseline-version: 0
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        enabled: ${NACOS_ENABLED:true}
        server-addr: 10.193.102.2:8848
      config:
        enabled: ${NACOS_ENABLED:true}
        server-addr: 10.193.102.2:8848
        file-extension: yml

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

triobase:
  internal:
    token: ${INTERNAL_SERVICE_TOKEN:triobase-local-internal-token}
    allowed-callers: ${INTERNAL_ALLOWED_CALLERS:platform-gateway,service-auth,service-org,service-lowcode,service-workflow-engine,service-openapi,service-billing}

logging:
  level:
    root: INFO
    com.triobase: DEBUG
