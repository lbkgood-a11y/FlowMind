server:
  port: ${BUSINESS_CATALOG_SERVICE_PORT:8090}

spring:
  application:
    name: service-business-catalog
  config:
    import: optional:nacos:${spring.application.name}.yml
  datasource:
    url: ${BUSINESS_CATALOG_DB_URL:jdbc:postgresql://localhost:5433/triobase}
    username: ${BUSINESS_CATALOG_DB_USERNAME:triobase}
    password: ${BUSINESS_CATALOG_DB_PASSWORD:triobase123}
    driver-class-name: org.postgresql.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: false
    table: flyway_schema_history_business_catalog
    baseline-on-migrate: true
    baseline-version: 0
  cloud:
    nacos:
      discovery:
        enabled: ${NACOS_ENABLED:false}
        server-addr: localhost:8848
      config:
        enabled: ${NACOS_ENABLED:false}
        server-addr: localhost:8848
        file-extension: yml

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

triobase:
  internal:
    token: ${INTERNAL_SERVICE_TOKEN:triobase-local-internal-token}
    allowed-callers: ${INTERNAL_ALLOWED_CALLERS:service-action,service-lowcode,service-workflow-engine,service-openapi,service-auth,platform-gateway}

logging:
  level:
    root: INFO
    com.triobase: DEBUG
